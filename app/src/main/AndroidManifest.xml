<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- [기존 주석] 잠금 화면 위로 Activity를 표시하고 기기를 깨우기 위한 권한 -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- [기존 주석] 진동을 사용하기 위한 권한 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- [기존 주석] 알람 예약 및 정확한 알람 실행을 위한 권한 (API 31+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- [기존 주석] Android 13 (API 33) 이상에서 알림을 표시하기 위한 권한 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alarm">

        <!-- [새로운 Activity 등록] 알람이 울릴 때 표시될 전용 화면입니다. -->
        <activity
            android:name=".AlarmRingingActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />
            <!--
                 [새 주석] 위 속성들의 역할:
                 - android:exported="false": 이 Activity는 우리 앱 내부(AlarmReceiver)에서만 호출할 수 있습니다.
                 - android:launchMode="singleInstance": 이 Activity의 인스턴스는 시스템 전체에서 단 하나만 존재합니다. 알람이 여러 번 울려도 화면이 여러 개 뜨지 않습니다.
                 - android:showWhenLocked="true": 기기가 잠금 상태일 때에도 이 Activity가 표시되도록 허용합니다.
                 - android:turnScreenOn="true": 이 Activity가 시작될 때 기기 화면이 켜지도록 합니다.
            -->

        <activity
            android:name=".SetAlarmActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AlarmReceiver 등록 -->
        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true"
            android:exported="false">
        </receiver>

        <!-- [기존 주석] 알람음 재생을 위한 Service 등록 -->
        <service
            android:name=".AlarmSoundService"
            android:exported="false" />

    </application>

</manifest>